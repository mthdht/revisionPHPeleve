<?php

/**
 * Exercice sur les variables
 *
 * @author matthieu dehondt
 *
 * Objectif:
 *
 *      Pour les différents types (string, int, float, array, bool), créer 1 variable de chaque type et afficher avec un var_dump()
 *      Le but est de vérifier que le type affiché est celui voulu
 *
 *      EXEMPLE: $nom = "matthieu     =>    resultat du var_dump() = string (8) "matthieu"
 */


// variable type string

var_dump();

// variable de type int

var_dump();

// variable de type float

var_dump();

// variable de type bool

var_dump();

// variable de type array

var_dump();
